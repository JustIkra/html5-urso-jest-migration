class ModulesScenesModel {
  loadUpdate(_loadProgress?: number): void {
    // blank lifecycle hook — overwritten by service.display()
  }

  preload(): void {
    // blank lifecycle hook
  }

  create(): void {
    // blank lifecycle hook — overwritten by service.display()
  }

  update(_deltaTime?: number): void {
    // blank lifecycle hook — overwritten by service.display()
  }

  render(): void {
    // blank lifecycle hook
  }

  destroy(): void {
    // blank lifecycle hook — overwritten by service.display()
  }
}

export default ModulesScenesModel;
